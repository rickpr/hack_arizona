<div style="display: none;">
  <% Category.all.each do |category| %>
    <%= select_tag :sub_category, options_for_select(category.sub_categories.map { |s| [s.name, s.id] }) %>
  <% end %>
</div>
<% Post.all.each do |post| %>
  <%= render "post", post: post %>
<% end %>
